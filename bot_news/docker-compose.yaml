version: '3'
services:
   bot:
        build: ./bot
        working_dir: /bot
        env_file:
                - .env
   searchengine:
        build: ./MeiliSearch
        ports:
           - 7700:7700

